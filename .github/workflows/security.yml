name: Security Scan

on:
  workflow_run:
    workflows: ['Tests', 'PR Validation']
    types:
      - completed
  pull_request:
    branches: [main, dev]
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

jobs:
  snyk-sast:
    name: Snyk Security Scan
    runs-on: ubuntu-latest
    # Only run if tests passed or on direct PR/workflow_dispatch
    if: |
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request') ||
      github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.pull_requests[0].head.sha || github.event.pull_request.head.sha || github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run Snyk Security Scan
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high

      - name: Upload Snyk results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true
        with:
          sarif_file: snyk.sarif
        # Note: Snyk SARIF output requires additional configuration

  semgrep-sast:
    name: Semgrep Security Scan
    runs-on: ubuntu-latest
    # Only run if tests passed or on direct PR/workflow_dispatch
    if: |
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request') ||
      github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.pull_requests[0].head.sha || github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0 # Full history for better analysis

      - name: Run Semgrep Security Scan
        uses: returntocorp/semgrep-action@v1
        with:
          config: >-
            p/security-audit
            p/owasp-top-ten
            p/typescript
            p/javascript
            p/react
            p/react-native
        continue-on-error: true

      - name: Upload Semgrep results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true
        with:
          sarif_file: semgrep.sarif

  security-summary:
    name: Security Scan Summary
    runs-on: ubuntu-latest
    needs: [snyk-sast, semgrep-sast]
    if: always() && github.event_name == 'pull_request'

    steps:
      - name: Security Scan Summary
        run: |
          echo "## Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Snyk Security Scan" >> $GITHUB_STEP_SUMMARY
          echo "- Status: ${{ needs.snyk-sast.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Semgrep Security Scan" >> $GITHUB_STEP_SUMMARY
          echo "- Status: ${{ needs.semgrep-sast.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Security scans completed. Review results above.**" >> $GITHUB_STEP_SUMMARY
